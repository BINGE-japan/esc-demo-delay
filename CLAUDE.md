# CLAUDE.md — このリポジトリでの作業ルール

このプロジェクトは Suara SDK 上の **ファズ／ハードクリップ系ディストーション**プラグイン。
開発はバイブコーディング × **ドキュメント至上主義**で進める。

## 最重要: ドキュメント駆動の3ルール

1. **実装前に必ず仕様確定** — コードを書く前に、対象が `docs/SPEC.md` / `docs/DSP.md` / `docs/ARCHITECTURE.md` に書かれていることを確認する。書かれていなければ先に仕様を書く。
2. **実装は常にドキュメント準拠** — 着手前に関連ドキュメントを読む。コードと仕様が食い違ったら、**コードでなくドキュメントを先に直す**（どちらが正しいかをユーザーに確認してから）。
3. **変更は必ず追記** — 仕様変更・設計判断は `docs/DECISIONS.md` に「日付＋決定＋理由＋影響」で追記し、影響する仕様ドキュメントを更新してからコミットする。値の SSoT（パラメータ範囲・ID 等）は片方を変えたら対応箇所も直す。

作業フローの目安: 関連 docs を読む → 必要なら仕様を更新（DECISIONS 追記）→ 実装 → `vp check` → docs と TASKS のチェックを反映。

## ドキュメントの地図（`docs/`）

| ファイル          | 中身                                              |
| ----------------- | ------------------------------------------------- |
| `SPEC.md`         | 製品仕様・パラメータ表・受け入れ基準（What）      |
| `DSP.md`          | 歪みアルゴリズムの数式・段構成（実装の核）        |
| `ARCHITECTURE.md` | ファイル構成・信号フロー・パラメータ橋渡し（How） |
| `DECISIONS.md`    | 決定ログ／変更履歴（**追記専用**）                |
| `TASKS.md`        | フェーズ別ロードマップと進捗                      |

迷ったら `docs/SPEC.md` → `docs/TASKS.md`（今どのフェーズか）の順で確認する。
ドキュメントの記述言語は **日本語**。

## ツール / ビルド

- パッケージ操作・ビルドは **`vp`（Vite+）**。npm/pnpm を直接使わない（[AGENTS.md](./AGENTS.md)）。
- `vp dev` 開発 / `vp build` 本番 / `vp check` fmt+lint+typecheck / `vp test` テスト。
- VST パッケージングはグローバル `suara` CLI（`vp run build:dev` 等）。
- worklet（`src/audio/worklets/`）は `tsconfig.worklet.json` 管理で **DOM lib 無し**。DOM API を worklet に書かない。
- SDK（`src/sdk/`）は vendored。原則編集しない。

## 現在地

フェーズは `docs/TASKS.md` を参照（2026-06-20 時点: Phase 0 完了、Phase 1 = MVP 着手前）。
